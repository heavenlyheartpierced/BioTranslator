enter codon string ex: "AJAUGAUUAGDKS"

String rnaStrand = scanner.nextLine().toUpperCase().trim()

StringBuilder proteinSequence = new StringBuilder();

for (int i = 0; i <= rnaStrnad.length() - 3; i += 3) {
    exclude codon
    check codon


}